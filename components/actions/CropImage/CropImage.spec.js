import { shallowMount, mount } from '@vue/test-utils';
import { vuetify } from '../../../plugins/vuetify';
import CropImage from './index';

describe('CropImage', () => {
    it('has ***', () => {
        const wrapper = mount(CropImage, { vuetify });
    });
});
